# Backend environment (copy to .env for local dev)
# Admin auth (defaults are same as below if unset, change for production)
ADMIN_EMAIL=aribdaniyal88@gmail.com
ADMIN_PASSWORD=daniyaldanish88
ADMIN_SECRET=daniyaldanish88

# Contact email recipient (defaults to ADMIN_EMAIL if unset)
CONTACT_RECIPIENT=aribdaniyal88@gmail.com

# SMTP configuration (set these for real email delivery)
SMTP_HOST=smtp.yourprovider.com
SMTP_PORT=587
SMTP_USERNAME=your_smtp_username
SMTP_PASSWORD=your_smtp_password
SMTP_USE_TLS=true

# Show verification code in API response when SMTP is not configured (DEV ONLY)
DEV_EXPOSE_VERIFY_CODE=true

# OAuth (dev mode works without these; set OAUTH_DEV_AUTO_LOGIN=true to auto-complete)
GOOGLE_CLIENT_ID=
GOOGLE_REDIRECT_URL=http://localhost:5173/oauth/google/callback
FACEBOOK_CLIENT_ID=
FACEBOOK_REDIRECT_URL=http://localhost:5173/oauth/facebook/callback
GITHUB_CLIENT_ID=
GITHUB_REDIRECT_URL=http://localhost:5173/oauth/github/callback
OAUTH_DEV_AUTO_LOGIN=true

# CORS
FRONTEND_ORIGIN=http://localhost:5173
